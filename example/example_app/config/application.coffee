module.exports = (done) ->
    # this method is called before sonea.bootstrap
    
    # configure whatever you want
    # and call done if finish
    
    # this = current instance of this app = sonea
    # @config = the current config object
    
    done()